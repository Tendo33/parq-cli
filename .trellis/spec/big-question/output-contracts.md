# Output Contracts

`plain` and `json` output modes are used by automation. Do not make them prettier at the cost of stable parsing.

If output shape changes, update README examples and tests in `tests/test_output.py`, `tests/test_plain_output.py`, and CLI workflow tests.
